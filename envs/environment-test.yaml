# conda environment description file for SalishSeaNowcast package testing environment
#
# Creates a conda environment in which the SalishSeaNowcast package unit tests and
# coverage analysis can be run.
# Primarily intended for creation of a conda env for use in a GitHub Actions workflow.

name: salishsea-nowcast-test

channels:
  - conda-forge
  - nodefaults

dependencies:
  - arrow
  - attrs
  - beautifulsoup4
  - bottleneck
  - cartopy
  - click
  - cliff
  - cmocean
  - coloredlogs
  - dask
  - docutils
  - f90nml
  - feedgen>=0.9.0
  - flox
  - geopandas
  - gitpython
  - gsw
  - lxml
  - mako
  - matplotlib
  - nc-time-axis
  - netcdf4
  - numpy
  - numpy-indexed
  - pandas
  - paramiko
  - pillow
  - pip
  - poppler
  - pygrib
  - pypdf2
  - pyproj
  - pytables
  - pyyaml
  - pyzmq
  - rasterio
  - requests
  - retrying
  - rich
  - schedule
  - scipy
  - scour
  - sentry-sdk
  - shapely
  - structlog
  - supervisor
  - tenacity
  - utm
  - verboselogs
  - watchdog
  - xarray

  # For unit tests and coverage monitoring
  - pytest
  - pytest-cov
  - tomli

  # For documentation links checking
  - sphinx
  - sphinx_rtd_theme
  - sphinx-notfound-page

  - pip:
    - angles
    - python-hglib
    - pytest-randomly
    - sysrsync

    # Install MOAD-ish dependencies and SalishSeaNowcast package in editable mode
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/43ravens/NEMO_Nowcast.git#egg=NEMO_Nowcast
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/UBC-MOAD/moad_tools#egg=moad_tools
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/UBC-MOAD/Reshapr#egg=Reshapr
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/SalishSeaCast/NEMO-Cmd.git#egg=NEMO-Cmd
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/SalishSeaCast/SalishSeaCmd.git#egg=SalishSeaCmd
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ "git+https://github.com/SalishSeaCast/tools#egg=SalishSeaTools&subdirectory=SalishSeaTools"
    - --editable --src $GITHUB_WORKSPACE/../vcs_pkgs/ git+https://github.com/SalishSeaCast/FVCOM-Cmd.git#egg=FVCOM-Cmd
    - --editable ../
