# conda environment description file for docs build environment
# on readthedocs.org

name: sphinx-build

channels:
  - conda-forge
  - nodefaults

dependencies:
  - matplotlib
  - pip
  - python=3.9
  - retrying

  # RTD packages
  - mock
  - pillow
  - sphinx
  - sphinx_rtd_theme

  - pip:
      - git+https://github.com/43ravens/NEMO_Nowcast.git#egg=nemo_nowcast
